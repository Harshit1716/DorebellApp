export interface loginRequestType {
    email: string;
    password: String;
}
export interface signUpRequestType {
    email: string;
    password: string;
    last_name: string
    first_name: any,
}